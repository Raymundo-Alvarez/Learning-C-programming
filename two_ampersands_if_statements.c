#include <stdio.h>
#include <stdlib.h>

int main()
{
	int hoursStudyPerWeek = 20;
	int hoursSleep = 8;

	if( (hoursStudyPerWeek) && (hoursSleep)) {
		printf("The student has good time manangement.");
	}
	
	return 0;
}